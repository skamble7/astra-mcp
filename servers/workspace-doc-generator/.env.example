# --- MCP Transport ---
MCP_TRANSPORT=stdio
MCP_SSE_HOST=0.0.0.0
MCP_SSE_PORT=8895

# --- Services ---
# NOTE: Use the direct endpoint form implemented below
ARTIFACT_SERVICE_URL=http://host.docker.internal:9020

# --- LLM (OpenAI) ---
ENABLE_REAL_LLM=true
LLM_PROVIDER=OpenAI
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0.2
LLM_MAX_TOKENS=1600
# Inject at runtime; don't commit real keys:
OPENAI_API_KEY=<OPENAI_API_KEY>

# --- Output ---
OUTPUT_DIR=/tmp/mcp-workspace-docs

# --- Logging ---
SERVICE_NAME=mcp.workspace.doc.generator
LOG_LEVEL=INFO