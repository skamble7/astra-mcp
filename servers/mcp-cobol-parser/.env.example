# ---- MCP Transport Configuration ----
MCP_TRANSPORT=streamable-http
MCP_HOST=0.0.0.0
MCP_PORT=8765
# Mount path for streamable HTTP (defaults to /mcp)
# MCP_MOUNT_PATH=/mcp

# For quick curl/browser tests (stateless JSON; not for production):
# MCP_STATELESS_JSON=true

# ---- paging & workers ----
PAGE_SIZE=100
MAX_PAGE_SIZE=500
WORKERS=8
CACHE_DIR=/app/.cache
LOG_LEVEL=INFO

# ---- CB2XML ----
CB2XML_JAR=/app/vendor/cb2xml/cb2xml.jar
# CB2XML_CP=/app/vendor/cb2xml/cb2xml_Jaxb.jar:/app/vendor/cb2xml/cb2xml_definitions.jar
# CB2XML_MAIN=net.sf.cb2xml.Cb2Xml

# ---- PROLEAP ----
PROLEAP_JAR=/app/vendor/proleap/proleap-bridge.jar
PROLEAP_CP=/app/vendor/proleap/lib/*
# Use the richer JSON bridge by default
PROLEAP_MAIN=com.astra.proleap.JsonCli

# ---- Strictness (NO fallback parsers) ----
STRICT_PROLEAP=1
STRICT_CB2XML=1

# Optional timeouts & formats
# COBOL_JAVA_TIMEOUT_SEC=60
# COBOL_SOURCE_FORMAT=FIXED